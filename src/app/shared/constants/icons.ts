// Si necesitas keys de iconos en string (por ejemplo para mapping en UI)
// OJO: el sistema real de iconos está en shared/core/icons/.
// Este archivo queda como placeholder para evitar imports rotos.
export const LV_ICON_KEYS = {
  // ejemplos
  home: 'home',
  user: 'user',
} as const;

