export const LV_ROUTES = {
  // Auth
  login: '/login',
  register: '/register',
  forgotPassword: '/forgot-password',

  // App
  dashboard: '/dashboard',

  // Fallback
  notFound: '/404',
} as const;

