import { DividerOrientation } from '../types/divider.types';

export const LV_DIVIDER_BASE =
  'border-gray-200';

export const LV_DIVIDER_ORIENTATION: Record<DividerOrientation,string>={

horizontal:'w-full border-t',

vertical:'h-full border-l'

};
